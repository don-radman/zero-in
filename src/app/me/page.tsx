// Member profile: panda portrait + flag, patch shelf, gravity + tier, share card,
// and the "what my panda knows" transparency screen (view/edit/delete every stored fact).
export default function MePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8">
      <h1 className="text-2xl font-bold">Your panda</h1>
      <p>TODO(P0): profile, patches, gravity; TODO(P1): what my panda knows</p>
    </main>
  );
}
