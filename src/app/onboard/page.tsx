// Onboarding: Privy email auth -> 4 questions + optional socials -> panda generation -> hatch.
// Every question carries microcopy: "helps your panda find your people."
export default function OnboardPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8">
      <h1 className="text-2xl font-bold">Your panda is waiting</h1>
      <p>TODO(P0): Privy auth, questions, hatch reveal</p>
    </main>
  );
}
