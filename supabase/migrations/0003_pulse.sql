-- One-time optional pulse per patch claim (+10 gravity): vibes multi-select,
-- made-connections yes/no, one improvement suggestion. Aggregate-only for hosts.
alter table patches add column pulse jsonb;
